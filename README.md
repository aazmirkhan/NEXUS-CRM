# NexusAi CRM

Mini CRM / lead pipeline dashboard for NexusAi's portfolio (CRMs category).
No backend — data is saved in the browser's localStorage.

## Local development
```
npm install
npm run dev
```
